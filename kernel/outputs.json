{
  "id": "kernel.outputs",
  "family": "outputs",
  "description": "Output contracts: the shape of what an agent delivers. Shape only — an output contract never grants permission (boundary.output_not_permission).",
  "entries": [
    {
      "id": "output.execution_report",
      "use_for": "Reporting implementation work.",
      "required_sections": ["issue or PR", "repository", "branch", "evidence reviewed", "files changed", "validation performed with results (or reason not run)", "risks and limitations", "commit or PR reference when applicable", "remaining work"],
      "rule": "Never claim done unless scope is complete, out-of-scope was respected, validation is reported, and review can verify the result."
    },
    {
      "id": "output.review_result",
      "use_for": "Reporting review or analysis findings.",
      "required_sections": ["scope reviewed", "evidence reviewed", "findings with file/line or issue/PR references", "verdict or recommendation", "risks", "what was not reviewed"]
    },
    {
      "id": "output.closure_comment",
      "use_for": "The GitHub comment that closes out an issue.",
      "required_sections": ["completion evidence (what now exists)", "validation evidence with real output", "accepted exceptions (a scoped PM-accepted validation/CI exception, or none)", "boundaries preserved (what was deliberately not done)", "friction note (one concise line, required)", "commit/PR references"],
      "rule": "This comment is the reconstruction packet for the next agent. Template: templates/closure-comment.md."
    },
    {
      "id": "output.draft_issue",
      "use_for": "Drafting GitHub issues for PM review.",
      "required_sections": ["why this exists (1-3 sentences)", "objective", "source basis", "scope", "out of scope (short)", "acceptance criteria", "validation expectations", "risk and rollback"],
      "rule": "Template: templates/issue.md. One outcome per issue. Body under ~5KB."
    },
    {
      "id": "output.route_prompt",
      "use_for": "Drafting a prompt that routes scoped work from a draft-only surface to a terminal agent.",
      "required_sections": ["the standard variable block (see adapters/BROWSER_CHAT.target.md)", "task instruction in terms of the issue scope", "recommended_effort with a 1-2 line rationale", "note that the executing agent re-resolves the kernel and that this prompt grants no permission"],
      "rule": "Route prompts carry scope and shape only. Authorization travels separately as exact scoped PM approval. A PM-approved scoped route prompt is approval evidence for that exact scope; the executing agent does not re-request authorization unless scope, actor, write type, target, risk, or evidence changes materially (see execution_modes approval_note). Copy-safe: plain text, at most one fenced block, no fenced blocks inside it (boundary.copy_safe_commands)."
    },
    {
      "id": "output.pm_command_bundle",
      "use_for": "Copy-safe command bundles the PM executes manually.",
      "required_sections": ["what the bundle does and its scope (prose, outside code blocks)", "the commands with exact targets in run order", "expected verification output", "risk and rollback (prose)"],
      "rule": "Names the contract sections only. The default short/linear shape, full style, disallowed patterns, when a defensive bundle is allowed, and examples are defined once in templates/commands/PM_COMMAND_BUNDLE.md; the hard floor is boundary.copy_safe_commands. Do not restate bundle rules here."
    },
    {
      "id": "output.handoff_packet",
      "use_for": "Transferring work between sessions, agents, or AI tools.",
      "required_sections": ["current state with live links (issues, PRs, branches)", "what was verified vs assumed", "next steps with their source basis", "open decisions for the PM", "boundaries active on the remaining work"],
      "rule": "Points to live GitHub evidence; never becomes a durable state file."
    },
    {
      "id": "output.adoption_packet",
      "use_for": "Bootstrapping a target project onto the kernel.",
      "required_sections": ["target repository", "kernel version adopted", "adapter file diff or full draft", "adoption checklist", "what stays target-owned (product truth)", "rollback (restore previous adapter)"]
    },
    {
      "id": "output.status_result",
      "use_for": "Returning needs_context, needs_pm_decision, or blocked.",
      "required_sections": ["the status", "exactly what is missing, conflicting, or blocking", "where it should come from or who decides", "the safe next step"],
      "rule": "Name the missing source or blocking boundary precisely; never pad with restated rules."
    }
  ]
}

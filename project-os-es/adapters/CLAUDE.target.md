# CLAUDE.md (shim para target)

Copia el bloque siguiente como `CLAUDE.md` después de instalar `AGENTS.md` desde
`project-os-es/adapters/AGENTS.target.md`.

---

# CLAUDE.md

Shim de Claude para `{{ORG/REPO}}`. Usa `AGENTS.md` como único bootloader
terminal del target. Este shim no repite metadata, resolver ni políticas; no
concede permisos ni guarda estado vivo.

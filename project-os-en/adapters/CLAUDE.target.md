# CLAUDE.md (target shim)

Copy the block below as `CLAUDE.md` after installing `AGENTS.md` from `project-os-en/adapters/AGENTS.target.md`.

---

# CLAUDE.md

Claude shim for `{{ORG/REPO}}`. Use `AGENTS.md` as the target's only complete terminal bootloader. This shim repeats no metadata, resolver instructions, or policy; it grants no permission and stores no live state.
